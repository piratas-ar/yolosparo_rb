- content_for :title do
  =@campaigns.first.title

- content_for :meta_name do
  =@campaigns.first.slogan

- @campaigns.each do |campaign|
  = render 'campaigns/_campaign', :locals => { campaign: campaign }
  %hr
